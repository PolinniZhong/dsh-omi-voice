# dsh-omi-voice 隐私说明

**一句话：豆包 API Key 永不进入本插件；所有语音合成都在你本机的 Omi 引擎内完成。**

- **Key 只存在 Omi 引擎**：豆包 API Key 由你在 Omi 引擎设置页填写，存入 macOS Keychain，本插件（运行在 DSH 网页里）没有任何 Key 输入入口，也看不到 Key。
- **仅本地通信**：插件只与 `127.0.0.1:8765`（Omi 引擎本地服务）通信，不向任何远程服务器发送文本。
- **文本不留存**：发送给引擎的文本仅在内存中用于合成播放；Omi 引擎不落盘、不写历史、不回调任何远程端点。
- **日志脱敏**：引擎日志只记录路由与状态（如 `localtts started port=8765`），不记录文本内容与 Key。

## 引擎侧（Omi）说明

- 本地 TTS 服务只监听 `127.0.0.1`，不接受局域网/公网连接。
- v1 协议无鉴权 token：本机任意进程可触发 TTS 朗读。影响面仅"本机扬声器出声"，无数据外泄面（Key 不透明、文本不回传）。
